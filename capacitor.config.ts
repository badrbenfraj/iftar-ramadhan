import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.iftar2024',
  appName: 'إفطار صائم',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
